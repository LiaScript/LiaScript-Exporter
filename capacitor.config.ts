import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.liascript.app',
  appName: 'LiaScript',
  webDir: 'dist/assets/indexeddb',
  bundledWebRuntime: false
};

export default config;
